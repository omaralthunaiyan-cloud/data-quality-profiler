# data-quality-profiler
Upload any spreadsheet, get an instant per-column data quality report — zero schema, zero config. Type inference + completeness/validity/uniqueness/outlier scoring in Python, plus a zero-install vanilla-JS port with PDF export.
